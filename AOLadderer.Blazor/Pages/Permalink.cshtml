@layout Layout

@page "/permalink"

@inject AppState Model
@inject IUriHelper UriHelper

@{ 
  Uri url = new Uri(UriHelper.GetAbsoluteUri());
  string token = url.Query.TrimStart('?');
  Model.Build.UrlTokenDeserialize(token);
  UriHelper.NavigateTo("/build");
}
