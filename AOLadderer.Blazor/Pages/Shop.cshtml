@layout Layout

@page "/shopping"

@functions {
    protected override Task OnInitAsync()
      => Task.Run(AppState.InitializeLadder);
}

@if (AppState.Shopping != null)
{
  <ShoppingComponent Model="AppState.Shopping"></ShoppingComponent>
}

<hr class="border-secondary" />

<NavLink href="/ladder" class="btn btn-primary btn-lg float-left">&lt; Ladder</NavLink>
